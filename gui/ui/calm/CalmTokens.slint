export global Tokens {
    in property <length> radius-sm: 4px;
    in property <length> radius-md: 6px;
    in property <length> radius-lg: 10px;
    in property <length> radius-window: 12px;
    in property <length> radius-island: 10px;

    in property <length> space-xs: 4px;
    in property <length> space-sm: 8px;
    in property <length> space-md: 12px;
    in property <length> space-lg: 16px;
    in property <length> space-xl: 24px;
    in property <length> space-xxl: 32px;

    in property <length> control-height: 28px;

    in property <length> label-size: 10px;
    in property <length> label-tracking: 0.8px;
    in property <length> body-size: 13px;
    in property <length> title-size: 15px;
    in property <length> brand-size: 36px;

    in property <length> main-min-width: 760px;
    in property <length> main-min-height: 520px;
    in property <length> new-project-width: 960px;
    in property <length> new-project-height: 520px;
    in property <length> paste-min-width: 200px;
    in property <length> paste-max-width: 420px;
    in property <length> paste-min-height: 180px;
    in property <float> paste-width-ratio: 0.32;

    in property <length> titlebar-height: 36px;
    in property <length> titlebar-leading: 0px;

    out property <length> ruler-thickness: 20px;
    out property <length> tool-button-size: 32px;
    out property <length> tool-icon-size: 18px;
    out property <length> row-icon-size: 18px;
    out property <float> disabled-opacity: 0.35;
    out property <length> tools-panel-width: space-xs * 2 + 2 * (tool-button-size + space-sm) + space-sm;
    out property <length> layers-panel-width: 276px;
    out property <length> layers-rail-width: 24px;
    out property <length> layer-thumb-width: 40px;
    out property <length> layer-thumb-height: 40px;
    out property <length> hover-card-width: 192px;
}
